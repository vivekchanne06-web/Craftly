"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mask = mask;
exports.redactOutside = redactOutside;
exports.overParams = overParams;
const index_js_1 = require("../anonymizer/index.cjs");
function isPlainObject(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
/** Mask a value, keeping object key names so a trace shows what was set. */
function mask(value) {
    if (isPlainObject(value)) {
        return Object.fromEntries(Object.keys(value).map((key) => [key, index_js_1.SECRET_PLACEHOLDER]));
    }
    return index_js_1.SECRET_PLACEHOLDER;
}
/** Copy `entry`, masking values outside `safeKeys`, so unknown keys fail closed. */
function redactOutside(entry, safeKeys) {
    if (!isPlainObject(entry))
        return entry;
    return Object.fromEntries(Object.entries(entry).map(([key, value]) => [
        key,
        safeKeys.has(key) ? value : index_js_1.SECRET_PLACEHOLDER,
    ]));
}
/**
 * Apply `redact` to every object argument, since `traceable` shapes a
 * multi-argument call as `{ args: [params, requestOptions] }`.
 */
function overParams(inputs, redact) {
    if (Array.isArray(inputs?.args)) {
        return {
            ...inputs,
            args: inputs.args.map((arg) => isPlainObject(arg) ? redact(arg) : arg),
        };
    }
    return redact(inputs);
}
